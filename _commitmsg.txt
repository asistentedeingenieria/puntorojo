v823: ajustes de UI por fotos (que la info se vea completa + niveles colapsados)

F1/F3 · Tarjeta de AVANCE FÍSICO en móvil: el nombre del apto YA NO se trunca
   ("APARTAMENT..."). La info se apila (nombre completo arriba; % y estado abajo)
   y el estado no se parte en dos líneas. (.apto-info en columna, .apto-name sin
   ellipsis en el breakpoint móvil.)

F2/F5 · Visor de PDF in-app (ESTADO DE FUERZA / Planillas): el título ya no se
   corta con "..."; se muestra completo (envuelve si es muy largo). Quitado el
   overflow:hidden+ellipsis+nowrap del header de _pdfAbrirVisor.

F3 · "HISTORIAL DE PLANILLAS CERRADAS": el conteo (·N) se movió al control
   (N · MOSTRAR/OCULTAR) y el título queda en una sola fila (ya no queda el número
   "feo" colgando en otra línea).

F4 · Listados de niveles COLAPSADOS por defecto (AVANCE POR APARTAMENTO: cobro/
   físico-cuadritos/materiales/pago). Antes _avNivelColapsado arrancaba desplegado;
   ahora arranca colapsado y se abre al tocar. (El AVANCE FÍSICO tab ya estaba
   colapsado por defecto via avanceUIState; sin cambios.)

Tests: ui_v823.cjs 13/13 (incl. unit de _avNivelColapsado/_avToggleNivel: default
colapsado + abre/colapsa al tocar); suite 51/51; valjs baseline.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
