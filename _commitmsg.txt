v956: niveles OFICIALES del Excel en COBRO + pagos VIVOS en el avance de LIQUIDACION

DOS reportes de Antonio (22-jul), mismos sintomas ("los % no se actualizan"), dos
causas distintas — ambas overrides congelados con prioridad absoluta:

1) COBRO - AVANCE POR APARTAMENTO: los cuadritos de los aptos si cambiaban pero el
   % del NIVEL no. CAUSA: el render usa p.cobro.avanceLevels/avanceTorres (el %
   "oficial" que dejo una subida de PDF VIEJA) con prioridad absoluta (~L30301), y
   el import del Excel AVANCE PR solo escribia avanceAptos.
   FIX: parseAvanceExcel ahora captura tambien el % de las filas TORRE/NIVEL del
   Excel (ponderado por costo — el numero que Antonio espera ver) y el import los
   guarda en avanceLevels/avanceTorres con sello avanceMetaTs; _mergeCobroProyecto
   propaga el juego mas nuevo entre dispositivos. Bonus: se acabo la diferencia
   "la app promedia parejo vs el Excel pondera por costo".

2) LIQUIDACION PROVEEDORES - AVANCE POR APARTAMENTO: apto 1202 con etapas 1-4
   PAGADAS 100% mostraba UN cuadrito. CAUSA: la vista usa a.pagoManual (override
   que dejo el importador de pagos v703-v733) con prioridad absoluta sobre los
   pagos reales (paidPct5), y NADA lo actualiza al pagar por el flujo normal.
   FIX: _pagoPcts5(p, apto) = por etapa, el MAYOR entre lo importado y lo pagado
   real (los pagos reales ganan; el historico importado que va por delante no se
   pierde). Las 4 lecturas (cuadritos, KPIs, % torre, % nivel) usan el helper.
   OJO: hay definiciones MUERTAS duplicadas de renderPlanillaReporte (35017/39523)
   — la activa es la ultima (41283); solo esa se toco.

Tests: avance_niveles_oficial_v956.cjs (10) + pago_manual_vivo_v956.cjs (8).
Suite completa 172 avancetest + 115 recetatest verde; validador blocks=27 errs=1.
_mergeCobroProyecto extendido de forma aditiva y auto-curativa (APP_SYNC queda en
909: clientes v953-v955 adoptan los valores nuevos via su propio state=merged y
el merge v956 re-propaga si un rezagado sube un juego viejo).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
