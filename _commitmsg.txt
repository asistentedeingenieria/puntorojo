v1006: historial de órdenes recibidas + el DETALLE deja de sacar del panel (con el diagnóstico bueno)

DETALLE DESDE BODEGA CENTRAL / PROYECTOS VARIOS — cuarto intento, ahora verificado
- v1002 quitó el cierre del panel, v1003 subió el z-index, v1004 escondió el panel. El de
  v1004 "funcionaba" pero dejaba el dashboard de fondo: para Antonio era igual de sacado.
- Verificado en el archivo: #modalPedidoDetalle es hijo DIRECTO de <body> y ninguna regla
  crea un contexto de apilamiento por encima — el z-index sí manda (el diagnóstico de v1004
  era equivocado). El modal va a 99000 sobre los paneles (98000) y el panel QUEDA VISIBLE
  detrás; al cerrar, el z-index vuelve a su valor.

HISTORIAL DE ÓRDENES DE COMPRA (pedido de Antonio)
- En ÓRDENES DE COMPRA solo quedan a la vista las que TODAVÍA no llegaron a la obra. Las
  recibidas se guardan en HISTORIAL DE ÓRDENES RECIBIDAS, colapsable, con el mismo patrón de
  pedidos y planillas (conteo + MOSTRAR/OCULTAR, preferencia recordada por proyecto).
- _ocPendienteDeRecibir(oc, pd) (PURA): deja de estar pendiente cuando se firmó SU entrega
  (pd.recepciones[oc.id], v996) o cuando el pedido entero quedó RECIBIDO. Las canceladas no
  cuentan como pendientes.

Tests: 3482 avancetest (1 suite nueva) + 115 recetatest. Validador en baseline.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
