v1003: el panel de PROYECTOS VARIOS se veía transparente y el DETALLE abría por debajo

DOS DEFECTOS DE APILADO, MISMA RAÍZ: mezclar capas a pantalla completa con modales normales.

1. El panel de PROYECTOS VARIOS salía translúcido encima del dashboard (foto de Antonio):
   se armó con .modal-bg + .modal, cuyo fondo es rgba(...,.5). Los paneles de este tipo se
   arman como el de BODEGA CENTRAL (v959): capa OPACA a pantalla completa (var(--paper)),
   sin clases de modal.

2. "DETALLE me sigue sacando" de bodega central: .modal-bg tiene z-index 100 y los paneles
   de bodega y varios 98000, así que el modal de detalle abría DEBAJO del panel opaco —
   invisible. Antes se disimulaba porque el botón cerraba el panel primero (que era el
   problema original que Antonio reportó). Ahora, abierto desde un panel, el detalle se pone
   en 99000 y closeModal le devuelve su z-index normal al cerrarse.

Tests: 3445 avancetest + 115 recetatest. Validador en baseline.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
