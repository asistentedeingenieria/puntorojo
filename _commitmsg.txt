v844: el admin puede ELIMINAR definitivamente una solicitud de anticipo CANCELADA (con tombstone)

Pedido del user: poder borrar las solicitudes CANCELADAS (como admin) y que no vuelvan a salir nunca.

- window.eliminarSolicitudAnticipo(solId): gate _antEsAdmin() (perms ['*']); solo estado 'cancelada';
  prConfirm danger; re-lee del state vivo tras el modal (stale-ref v769/v770); agrega tombstone
  (state.solicitudesAnticipoEliminadas[id]=ts), borra los archivos de Storage best-effort
  (refFotoUrl/cotArchivoUrl), quita del array y persiste con _antSolicSave (saveState+forceUploadNow).
- applyRemote: el merge de solicitudesAnticipo pasó de _mergeById(..., null) a usar un tombstone
  (_solAnTomb = union local U remoto de solicitudesAnticipoEliminadas) — EXACTO patrón de anticipos
  (v752, anticiposEliminados). Sin esto la solicitud reaparecía: solicitudesAnticipo se une por id y
  un dispositivo con copia vieja la revivía. Converge en una vuelta (no hay loop: el id se quita
  genuinamente y el tombstone lo mantiene fuera).
- _antSolicRender: botón ELIMINAR (rojo) solo para estado 'cancelada' y _antEsAdmin().

Test: _avancetest/anticipo_eliminar_cancelada.cjs (12). Suite completa verde. valjs baseline
(blocks=27 errs=1).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
