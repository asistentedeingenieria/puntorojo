v949: candados de receta/pedidos/OC (solo admin) + permiso de receta = proponer

Pedido de Antonio (entrega 1 de 2; la edicion POR MODELO va en v950).

RECETA:
- CARGAR RECETA (EXCEL) = SOLO admin (data-perm users.manage + gate del handler).
- Se quita el boton PRECIOS de la barra de receta. El CATALOGO DE PRECIOS (pestaña
  ORDENES, openCatalogoProveedores) es la misma fuente y queda como unico lugar
  para editar precios.
- El permiso de receta pasa a ser PROPONER: quien tiene receta.edit ya no edita
  directo, sino que SOLICITA cada cambio (agregar / editar cantidad / eliminar).
  Solo el admin (users.manage) aplica directo (recetaV2Op) y ve el boton CAMBIAR
  de postes. Nadie sin permiso ve los controles (+AGREGAR / ✎ / ✕).
- Autorizar/rechazar solicitudes de receta = receta.autorizar o admin (antes
  receta.edit las autorizaba). Permiso nuevo receta.autorizar en USUARIOS; el
  label de receta.edit ahora dice "Proponer cambios...".

BORRADO SOLO ADMIN:
- Eliminar PEDIDOS: la X de la tarjeta y el boton ELIMINAR PEDIDO del modal ahora
  solo los ve el admin (antes: users.manage || pedidos.create || pedidos.advance).
- Eliminar ORDENES DE COMPRA: el boton ✕ y deleteOrden solo admin (antes:
  compras.autorizar || users.manage). Se separo canDelete en canFactura (compras,
  para no romper SUBIR FACTURA de v947) y canDeleteOC (solo admin).

No toca la logica de sync (no sube APP_SYNC_VERSION). Test candados_v949.cjs (21).
Suite completa 163 avancetest + 115 recetatest verde; validador blocks=27 errs=1.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
