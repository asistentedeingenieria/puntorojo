v1004: DETALLE deja de sacar de bodega y + NUEVO PEDIDO de varios funciona

DETALLE EN BODEGA CENTRAL — tercer intento, ahora con la causa real
- v1002 quitó el cierre del panel; v1003 le subió el z-index al modal. Ninguno alcanzó: el
  modal de detalle vive DENTRO de un contenedor, así que su z-index no escapa de ese
  contexto de apilamiento y seguía quedando debajo de la capa opaca del panel.
- Solución que no depende del apilado: mientras el detalle está abierto se ESCONDE el panel
  (display:none) y closeModal lo devuelve. Al cerrar, el usuario sigue en bodega central.

+ NUEVO PEDIDO EN PROYECTOS VARIOS (reporte: "me regresa a proyecto y no me deja hacer pedidos")
- CAUSA: llamaba a showSection() y setMaterialesTab(), que NO EXISTEN en la app. El try/catch
  se comía el ReferenceError y lo único que quedaba hecho era el cierre del panel.
- Los nombres reales son setView('materiales') y setMatTab('pedidos'). Se verificó que las
  cuatro funciones que usa existen, y el test lo fija para que no vuelva a pasar.
- Además deja el formulario en modo PROYECTO PEQUEÑO, hace foco en el campo del cliente y
  avisa qué hacer.

Tests: 3450 avancetest + 115 recetatest. Validador en baseline.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
