v821: 6 cambios de UI/comportamiento (pedidos del user)

A1 · Quitado el botón "CARAS SIN REGISTRAR" de ASISTENCIA (ya no se usa).
   La función _abrirDiagnosticoCaras queda definida pero sin acceso desde la UI.

A2 · "Quién cobra en planilla de obra" (OPT-IN). El combo PERSONA A PAGAR
   (PAGOS POR ETAPA) ahora SOLO muestra a los marcados:
   - _personalPickList expone pagoObra:p.pagoObra===true.
   - _getColabs del combo (IIFE) filtra por c.pagoObra (arranca vacío).
   - Check discreto "COBRA EN PLANILLA DE OBRA" en el detalle de COLABORADORES
     (solo OBRA + quien puede editar) -> _togglePagoObra (saveState+forceUploadNow,
     sella _ts para el merge multi-dispositivo).

A3 · Botón "PREGUNTÁ" (IA) -> ícono de chat SOLO, sin texto, 34x34 circular
   transparente (igual a la campana). SVG a 20px.

B4 · Avance físico: una etapa marcada YA NO se desmarca. Solo gerente/admin
   (planilla.authorize||users.manage) desmarca con confirmación; el encargado
   NO puede (ni mandar solicitud). _avanceToggleAccion: marcada+NO gerente -> 'locked'
   (antes 'request-unmark'); toggleStage muestra toast. Tooltip de etapa marcada
   pasó a "ETAPA COMPLETADA".

B5 · Modal de fotos en SOLO LECTURA cuando el apto está al 100%
   (_aptoTerminado = aptoCompleted(a)===6; _ro = !canEdit || _aptoTerminado):
   se ven las fotos pero NO se suben/eliminan. Banner "APTO TERMINADO · SOLO LECTURA".

B6 · Rediseño del modal de fotos: header de etapa centrado con número en círculo,
   bloques con esquinas redondeadas (10px) + sombra suave, slots 8px.

Tests: _avancetest/ui_v821.cjs 23/23; desmarcar.cjs 8/8 (actualizado a v821);
suite 48/48; valjs baseline (blocks=27 errs=1, falso positivo conocido).
Revisión adversarial (workflow) aplicada.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
