v885: tooltip legible en el "?" + X sin fondo (afinado del rediseno v884)

Dos ajustes pedidos por el user sobre la tarjeta de liquidacion:
1. El "?" del bloque de descuentos ya no usa el title nativo del
   navegador (ilegible): tooltip propio .pr-tip con data-tip via CSS
   (:hover::after content attr) - caja oscura, 280px, legible.
2. La X de quitar descuento queda SIN fondo ni borde (solo la X roja),
   mismo handler eliminarDescuentoPlanilla.

TDD: descuentos_rediseno_v884.cjs +2 aserciones (11). Suite completa
verde. valjs baseline.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
