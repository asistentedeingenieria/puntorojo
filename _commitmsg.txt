v867: quitar la sub-pestaña PLANILLAS OC de la vista PLANILLAS

El user pidió eliminar por completo la sub-pestaña PLANILLAS OC (órdenes de
cambio); las OC se van a rehacer después.

- Se elimina el botón del tab (data-plantab="ocs") del HTML.
- injectPlanillaTabs oculta cualquier botón 'ocs' que venga del cache (mismo
  patrón belt-and-suspenders que ajustes/excepciones en v345).
- El CÓDIGO OC queda DORMIDO, no se borra: renderPlanillaOCs y el panel
  #planilla-ocs siguen ahí (ocultos, sin punto de entrada) para que el
  rehacer sea limpio. Nada más navega a 'ocs' (el default es 'etapas').

TDD: _avancetest/quitar_planillas_oc.cjs (5). Suite completa verde. valjs
baseline.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
