v960: buscadores UNIFICADOS en toda la app + bodega central GENERAL

BUSCADORES (pedido de Antonio 23-jul, "que se vean todos iguales, simetricos
y limpios"): habia 6 familias visuales distintas conviviendo. Se adopta el
estandar que ya existia (v830/.pr-combo): 40px de alto, borde var(--line),
radius 8, mayusculas, foco rojo con halo.

- Clases nuevas .pr-buscador (inputs de busqueda) y select.pr-filtro (selects
  de filtro) en bloque CSS propio al FINAL del head (gana la cascada al
  safety-net) + variantes movil (44px) y modo oscuro.
- Aplicadas en TODOS los sitios vivos: avanceSearch, catalogoSearchInput,
  persBuscar/asistBuscar/gerBuscar (dejaron el look .inline sin borde),
  asistFecha, persEmpresaFilter, asistObraPdf, inboxFilter, usersBuscar,
  catProvSearch, polSearchInput, colabSearchInput, v372PersonaBuscadorInput,
  polPersonaBuscador, _prPickerBusca, filtros de polizas (ESTATUS/
  ASEGURADORA), anticipos (ESTADO), LIQUIDACIONES OC (ESTATUS RENGLON +
  BUSCAR OC #), factory _v391Combobox (modales anticipo/poliza).
- selStyle (3 vistas de descuentos/control) pasa de #E2E8F0 a var(--line).
- Desplegables ordenados y compactos: proveedores del picker v925 A-Z
  (encabezan la opcion vacia y BODEGA CENTRAL), panel del picker a max-height
  280px con borde var(--line) radius 8.
- Fuera del alcance a proposito: navbar (regla v830), editores .inline de
  tablas, kiosko oscuro.

BODEGA CENTRAL GENERAL (feedback de Antonio sobre v959):
- UN solo boton en la toolbar (BODEGA CENTRAL); ABASTECER vive dentro del
  panel.
- _bodegaProductosGlobal: el catalogo de compra une las recetas de TODOS los
  proyectos (dedupe por clave canonica, totales sumados) — lo usan el modal
  ABASTECER y las filas del panel.
- Recepcion multi-proyecto: _bodegaFindOc busca la OC en todos los proyectos;
  el panel gana la seccion POR RECIBIR (OCs de abastecimiento autorizadas sin
  entrada, de cualquier proyecto) con boton MARCAR RECIBIDO.
- El despacho ya descontaba del saldo GLOBAL (v959) — sin cambios de datos ni
  de sync (APP_SYNC queda 910).

Tests: buscadores_unificados_v960.cjs (29) + bodega_global_v960.cjs (14);
actualizados a la nueva realidad dropdowns_v830, oc_picker_buscable_v925,
bodega_central_v959, bodega_lista_abastecer_v936. Suites completas: 178
avancetest + 115 recetatest en verde; validador blocks=27 errs=1 (baseline).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
