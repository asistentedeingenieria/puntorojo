v822: anticipos (cuotas + permiso solo-ver) + limpieza modal de fotos + fix recursión

(1) Tarjeta de solicitud de anticipo: muestra cuántas CUOTAS serían (sol.cuotas, o
    la regla v416 por el monto cotizado): "COTIZACIÓN: ... · N CUOTA(S)" (auto plural).

(2) Permiso nuevo 'anticipos.verEstado' = SOLO ver el status de los pedidos, sin
    pedir / cotizar / autorizar / marcar entregado / cancelar.
    - window._antSoloVer(): tiene verEstado y NINGÚN permiso de acción/gestión.
    - Reusa la maquinaria descuentos-only (_v404b): al entrar a PLANILLAS ve SOLO la
      sub-pestaña ANTICIPOS, y dentro solo SOLICITUDES (sin RESUMEN), en modo lectura.
    - setView('planilla') deja entrar sin view.planilla; la nav-tab se re-muestra en
      applyPermissions. KPIs gateados por kpis.planilla (no se le dan).
    - Todos los cambios NO-OP para los demás roles.

(3) Limpieza del modal de fotos (avance físico), a pedido del user:
    - Quitado el subtítulo bajo el título (TORRE·NIVEL·APTO).
    - Quitado "UMBRAL ≥X% · ALCANZADA/PENDIENTE" de cada etapa (queda solo el aviso
      "TERMINAR LA ETAPA ANTERIOR PRIMERO" cuando está bloqueada).
    - Quitado "MÍNIMO 2 PARA COMPLETAR ETAPA" del cuadro de foto.

(4) FIX (hallado por la revisión adversarial): _v404bAplicarFiltroDescuentosTabs
    decidía con el .active del DOM (que se actualiza DESPUÉS de injectPlanillaTabs),
    así que al forzar 'anticipos' se re-llamaba sin fin -> recursión / stack overflow.
    Ahora decide con window.currentPlanillaTab (seteado ANTES). Bug latente que también
    afectaba al rol descuentos-only; el fix lo cura para ambos.

Tests: anticipos_verestado.cjs 18/18 (incl. unit de _antSoloVer + guard anti-recursión);
ui_modal_limpio.cjs 7/7; flujoanticipos 70/70; suite 50/50; valjs baseline.
Dos revisiones adversariales (workflows) aplicadas; ambas sin hallazgos confirmados tras el fix.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
