v1007: PROYECTOS VARIOS autónomo + historial de recibidos en los paneles

TODO SE MANEJA DENTRO DEL PANEL (pedido de Antonio: "en proyectos varios los pedidos y TODO
se maneje únicamente dentro de esta pestaña. NO QUIERO QUE ME REGRESE A LA GENERAL")
- + NUEVO PEDIDO ya no navega a la pestaña de materiales: se MUEVE el nodo real del
  formulario (#pedido-nuevo) adentro del panel y se devuelve a su lugar al salir. No se
  duplica nada; los handlers son onclick inline y sobreviven al movimiento.
- Queda fijo en PROYECTO PEQUEÑO (el selector se deshabilita: desde acá siempre es varios).
- Botón VOLVER AL LISTADO, y al enviar el pedido vuelve al listado de varios, no al proyecto.
- Cerrar el panel devuelve el formulario ANTES de destruirlo: si no, #pedido-nuevo se iría
  del DOM con el panel y la pestaña de materiales quedaría sin formulario.
- GENERAR OC tampoco cierra el panel: el modal de la orden se abre encima (verificado que
  #modalOrdenCompra es hijo directo de <body>, igual que el de detalle).

HISTORIAL DE RECIBIDOS EN LOS PANELES (pedido de Antonio: "que se oculten todas como
historial las ya recibidas")
- BODEGA CENTRAL y PROYECTOS VARIOS ocultan sus pedidos ya recibidos en un bloque colapsable
  con conteo y MOSTRAR/OCULTAR, igual que la pestaña de órdenes (v1006) y la de pedidos.
- _pedidoYaRecibido(pd) (PURA): cuenta como recibido si su estado es RECIBIDO o si tiene
  recepción firmada SIN entregas pendientes (v996) — un pedido a medias no se archiva.

Tests: 3507 avancetest (1 suite nueva) + 115 recetatest. Validador en baseline.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
