v964: el ABASTECIMIENTO vive en la BODEGA CENTRAL, no en los proyectos

Pedido de Antonio (23-jul): "cuando pido un pedido para abastecimiento me lo
tira para un proyecto en especifico. Quiero que no sea asi. Quiero que sea
para bodega central" — el descuento al proyecto queda SOLO via despacho de
bodega (SALIDA al autorizar, v959, sin cambios).

STORE GLOBAL state.bodegaMat {pedidos, ordenes}:
- Union-merge por id + _ts con tombstones pedidosEliminados/ordenesEliminadas
  en applyRemote (bloque v752) + memorias ocProvPorItem/postesPrecioMedida
  propias (union simple). APP_SYNC_VERSION 910 -> 911.
- Numeracion propia DERIVADA del maximo existente: 'BODEGA – 00001' y folios
  de OC sin contador mutable (leccion v953: dos dispositivos generando a la
  vez se pisan el correlativo). _projSiglas/'_ocNumeroPartes' compatibles
  ('BODEGA' es una palabra: sale entero en la hoja).
- Toda mutacion del store se sella con _ts (autorizar, factura, recibido,
  status del pedido); borrados escriben tombstone.

RESOLUCION GLOBAL en el circuito (bodega primero, proyecto despues):
- _findPedidoGlobal + _bodegaFindOc ampliado; getPedidoOrdenes concatena el
  store (los gates v927 y allPedidoOcsAuthorized ven las OCs de bodega).
- openPedidoDetalle, openOrdenCompra (con folio derivado y re-lectura v940),
  generarOrdenCompra (push al contenedor del pedido, notifs con nombre BODEGA
  CENTRAL y projectId null, cierre hacia la vista de bodega), advancePedido,
  printPedido, deletePedido/_doDeletePedido (tombstone), deleteFromDetalle,
  autorizarOrden (lookup + re-lectura + notif del solicitante), printOrdenCompra,
  previewOrden, _ocAbrirSubirFactura/_ocSubirFactura, _doDeleteOrden (tombstone),
  _ocEsAbastecimiento, _bodegaOcsPorRecibir (store primero; proyectos quedan por
  compatibilidad con abastecimientos viejos).
- autoAssignOcProviders: memoria del store para abastecimientos (semilla: la
  del proyecto activo).
- _ocConfirmarRecibido: cuando TODAS las OC del pedido del store tienen
  ENTRADA, el pedido pasa a RECIBIDO solo.

VISTA BODEGA CENTRAL: seccion PEDIDOS DE ABASTECIMIENTO (generar OC,
autorizar v919 con firma, ver borrador, imprimir, eliminar solo admin) +
re-pintado tras autorizar/recibir/borrar.

Tests: bodega_store_v964.cjs (31); actualizados a la nueva realidad
bodega_lista_abastecer_v936, oc_factura_v947, oc_stale_ref_v940. Suites: 181
avancetest + 115 recetatest verde; validador blocks=27 errs=1.

RITUAL: tras verificar el dominio, subir minSyncVersion a 911.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
