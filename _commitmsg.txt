v975: paquete de pedidos de Antonio (25/26-jul) — solicitud de medida de postes,
permiso de inventarios, sub-pestanas de MATERIALES ocultables, textos y orden

1. MEDIDA DE POSTES por solicitud: el proponente ve PROPONER CAMBIO
   (solicitarPostesMedida encola tipo 'postes' PENDIENTE, sin aplicar); el cambio
   directo (configurarPostesMedida) queda SOLO admin; autorizarReceta aplica la
   medida del nivel al aprobar (misma escritura que el admin).
2. inventario.crear: INICIAR NUEVA TOMA de inventario solo con el permiso
   (+admin) — boton y funcion gateados (antes no tenia gate).
3. Sub-pestanas de MATERIALES ocultables POR USUARIO: 5 permisos ocultarmat.*
   (grupo OCULTAR EN MATERIALES; prefijo a proposito para no abrir la pestana
   por la regla implicita de can()). Default = TODAS visibles; Antonio marca las
   que quita. Lectura LITERAL de perms (el '*' del admin no debe ocultarle nada);
   applyPermissions aplica tras el pase data-perm; setMatTab salta a la primera
   visible. FIX en el camino: try sin cerrar rompio el bloque #5 del validador
   (errs=2) — detectado por baseline antes de desplegar.
4. Aviso de receta en MAYUSCULAS + 'SOLICITUD ENVIADA' aclara que el cambio NO
   se aplica hasta aprobarse (tambien para AGREGAR, que ya funcionaba asi).
5. Texto de BODEGA CENTRAL reformulado (pedido explicito: el ajuste ya no es el
   conteo inicial): CARGAR EXISTENCIAS anota lo fisico, ABASTECER compra,
   AJUSTE solo correcciones con motivo.
6. Orden de sub-pestanas de pedidos: PEDIR DE RECETA · + NUEVO PEDIDO · LISTA.

Tests: materiales_v975.cjs (21) + receta_ver_precios_v971 ampliado (21).
Suites: 188 avancetest + 115 recetatest verde; validador blocks=27 errs=1.
Sin cambios de sync.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
