v968: CATALOGO DE COMPRAS — el match Excel aprobado por Antonio, cableado a la bodega

Decisiones de Antonio (23-jul, via la pagina de revision): 81 matches unicos,
50 AMBIGUOS = entran TODAS las variantes (cada una es material propio, se elige
al pedir), 16 SIN EQUIVALENTE = quedan con su nombre interno.

- const CATALOGO_COMPRAS (147 grupos, 246 nombres de COMPRA del Excel
  DATOS_COMPRAS). Generada por script desde el match adjudicado — NO editar a
  mano; se regenera si Antonio manda un Excel nuevo.
- _bodegaProductosGlobal: la BASE ahora es el catalogo de compras (el interno
  viaja como ALIAS de busqueda); las recetas de todos los proyectos se unen
  encima (aportan unidad/total sugerido). CARGAR EXISTENCIAS y ABASTECER listan
  los ~246 nombres de compra; la busqueda pesca tambien por el nombre interno.
- ABASTECER gano buscador propio (246 materiales sin filtro era un castigo).
- _ncDeCompra + buildPedidoOcItems: las OC de obra salen con el nombre de
  compra del Excel cuando el match es UNICO; los TODOS quedan con el interno
  (la variante se elige a mano).
- _ocItemMemKey (clave canonica): comillas tipograficas = rectas y '2 1/2' =
  '2½' (identidad receta<->Excel: sin esto el despacho no descontaria el stock
  cargado con nombre del Excel). El colapso X n' -> X 10' quedo SOLO para
  (MEDIDA ESPECIAL): antes se comia TODOS los largos en pies y regla de 7' y
  de 8' son stock distinto. Ojo: ocProvPorItem re-aprende proveedores para las
  claves que cambiaron (bodegaMovs/bodegaItemsGlobal estaban vacios hoy).

Sin cambios de sync (APP_SYNC queda 911). Tests: bodega_catalogo_compras_v968
(22); actualizados bodega_central_v959 (clave sin colapso) y precios_receta_v908
(stub _ncDeCompra). Suites 184 avancetest + 115 recetatest verde; validador
blocks=27 errs=1.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
