v1002: PROYECTOS VARIOS + eliminar OC pendientes + DETALLE no saca de bodega

PROYECTOS VARIOS (pedido de Antonio: "que haya una pestaña en PROYECTO que diga PROYECTOS
VARIOS y ahí poder manejar todos los pedidos de material de estos proyectos")
- Tercer contenedor con el patrón probado de BODEGA CENTRAL (v964): state.variosMat con sus
  pedidos, órdenes y tombstones, union-merge por id + _ts (+ _mergeRecepciones para las
  entregas por OC). APP_SYNC_VERSION 914 → 915.
- Entrada PROYECTOS VARIOS en el desplegable de PROYECTO (gate _puedeVerVarios) con su panel:
  pedidos agrupados por cliente, generar/autorizar/imprimir OC, detalle y + NUEVO PEDIDO que
  abre el formulario ya en modo proyecto pequeño.
- Un pedido MANUAL ya no se guarda dentro del proyecto que estuviera abierto. Los manuales
  viejos NO se migran: la vista los agrega igual (_variosPedidosTodos) para no romper las OCs
  que los citan, y la serie del cliente se cuenta sobre ambos (TIFFANY – 2 sigue a TIFFANY – 1).

FRONTERAS CERRADAS (un mapa previo de enganches marcó 6 críticas; sin ellas el pedido se
vuelve invisible, no se puede borrar o su OC se duplica):
- _findPedidoGlobal, getPedidoOrdenes y _bodegaFindOc resuelven los TRES contenedores.
- _doDeletePedido y _doDeleteOrden tienen su rama con tombstone (si no, el borrado se
  aplicaba sobre el proyecto abierto y el registro revivía en el próximo sync).
- deleteOrden buscaba solo en el proyecto activo: el ✕ de una OC de bodega o de varios no
  abría ni la confirmación.
- El folio previsto, la base de folios y la memoria de proveedor/precio salen del CONTENEDOR
  del pedido (_ctx.cont) en vez del ternario binario bodega/proyecto.
- El panel entra en isUserBusy: applyRemote no pisa referencias con el modal abierto.

OTROS DOS REPORTES DEL MISMO TRAMO
- Se pueden ELIMINAR las OC pendientes de autorizar (_ocEliminarBorrador): solo mientras
  finanzas no las firme, con tombstone, y si el pedido queda sin órdenes vuelve a SOLICITADO
  para poder generarlas de nuevo.
- DETALLE en un pedido de bodega ya no cierra el panel ni manda a las órdenes del proyecto.

Tests: 3438 avancetest (1 suite nueva) + 115 recetatest. Validador en baseline.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
