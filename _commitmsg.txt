v842: entrega de anticipo con factura+carta firmada (irreversibles) + foto opcional en la solicitud

v841 — GATE DE DOCUMENTOS ANTES DE ENTREGAR (sub-pestaña SOLICITUDES, estado AUTORIZADA):
- Antes de MARCAR ENTREGADO hay que subir, en secuencia: PASO 1 FACTURA del producto y
  PASO 2 CARTA FIRMADA de recibido. La carta queda bloqueada hasta tener la factura; el botón
  ENTREGAR queda deshabilitado hasta tener AMBOS (candado también en la lógica de
  entregarSolicitudAnticipo, no solo en la UI).
- Documentos IRREVERSIBLES: una vez subido, solo queda VER; sin reemplazar ni borrar. Salida de
  emergencia SOLO admin (perms ['*']) → REEMPLAZAR (ADMIN). Confirmación "¿es el documento
  correcto? no se podrá modificar" antes de cada subida.
- Suben solo compras (anticipos.cotizar) o gerente. Archivos a Firebase Storage
  (anticipos-factura/ y anticipos-carta/), URLs en la solicitud; VER queda visible tras ENTREGADA.
- Helper puro _antDocsListos(sol) (factura && carta) + _antEsAdmin().

Fixes de la revisión adversarial (carreras de sync en la subida concurrente):
- #1/#2: re-validar irreversibilidad y secuencia factura→carta TRAS la subida (3er re-read),
  borrando mi propia url (_borrarMiUrl) si abortamos.
- #5: borrar el archivo viejo SOLO si sigue siendo el que yo venía reemplazando (oldUrl===urlPrevia),
  para no borrar el de otro equipo.
- #4: los modales de anticipo (_antModalWrap → clase pr-ant-modal) entran a isUserBusy → applyRemote
  se posterga durante la subida.
- Regresión de #4 (2da revisión): los aborts dejaban el modal abierto y congelaban el sync;
  ahora _cerrarDoc() cierra el modal en todos los aborts.
- Residual aceptado: LWW de _mergeById puede dropear un doc en carrera admin-vs-admin (mismo patrón
  que todas las solicitudes; recuperable re-subiendo; no se borra ningún archivo vivo).

v842 — FOTO DE REFERENCIA OPCIONAL en SOLICITAR ANTICIPO:
- crearSolicitudAnticipo ahora es async: input #antSolFoto (opcional), sube best-effort a
  anticipos-solicitud/ y guarda refFotoUrl/refFotoNombre. Si no hay foto o falla, la solicitud se
  crea igual. Compras la ve en la tarjeta y en el modal SUBIR COTIZACIÓN (VER FOTO DE REFERENCIA).

Tests: anticipo_docs_entrega.cjs (34), anticipo_docs_race_fix.cjs (10), anticipo_solicitud_foto.cjs (17).
Suite completa verde. valjs baseline (blocks=27 errs=1).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
