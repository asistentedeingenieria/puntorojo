v873: pestana "Panel" + favicon neutro + splash embellecido + sin VINCULAR NOMBRES

1) Pestana del navegador: <title>Panel</title> (elegido por el user) y
   metas description/apple-mobile-web-app-title = Panel.
2) Favicon neutro propio (SVG data-URI, cuadrado gris redondeado) para
   reemplazar el globo por defecto que quedo al quitar el logo en v870.
3) Splash de carga mas bonito (sin marca): overlay radial elegante sobre
   el slideshow, spinner fino blanco 46px con easing suave, texto de
   estado con tracking amplio y pulso sutil.
4) POLIZAS: sub-pestana VINCULAR NOMBRES eliminada del menu (el user ya
   vinculo todos los actuales). El render _polView===vincular queda
   dormido; para reactivar, restaurar el boton comentado (v873).

Tests: ui_v873.cjs (7) + 2 asserts actualizados (anonimizar_marca title
Panel, vincularnombres boton removido). Suite completa verde. valjs
baseline.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
