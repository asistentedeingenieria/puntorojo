# Archivos del sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/
.idea/
*.swp

# Secretos y notas locales (NO subir a GitHub)
firebase-config.txt
*.local
*.secret

# Carpetas viejas de deploy manual (ya no se usan con auto-deploy)
puntorojo-deploy/

# Backups y temporales
*.bak
*.tmp
*~

# Node / Capacitor
node_modules/
npm-debug.log*
yarn-debug.log*
.npm/

# Android (Capacitor genera la carpeta android/, pero algunos artefactos no van a git)
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/app/release/
*.aab
*.apk
*.keystore
*.jks

# iOS (cuando llegue)
ios/App/Pods/
ios/App/build/
ios/.gradle/

# Trigger files
.pages-rebuild-trigger

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
