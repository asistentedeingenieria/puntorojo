{
  "name": "puntorojo",
  "version": "1.0.0",
  "description": "Punto Rojo · App de gestion de obra (drywall) — wrapper Capacitor para Android e iOS sobre la PWA en https://puntorojo.app",
  "private": true,
  "type": "module",
  "scripts": {
    "cap:sync": "cap sync",
    "cap:add:android": "cap add android",
    "cap:add:ios": "cap add ios",
    "cap:run:android": "cap run android",
    "cap:open:android": "cap open android",
    "assets": "capacitor-assets generate",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.1",
    "wrangler": "^4.92.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  }
}
